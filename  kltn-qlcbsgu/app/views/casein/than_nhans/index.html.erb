<!-- Scaffolding generated by Casein v.3.1.11 -->
<div class="search_basic_form">
  <%= form_tag casein_than_nhans_path, :method => "get" do %>
      <%= text_field_tag "keyword", params["keyword"] || "Nhập vào Họ tên thân nhân/Họ tên cán bộ/Mã cán bộ", {:title => "Nhập vào Họ tên thân nhân/Họ tên cán bộ/Mã cán bộ", :onfocus => "if(this.value == 'Nhập vào Họ tên thân nhân/Họ tên cán bộ/Mã cán bộ') {this.value=''}", :onblur => "if(this.value == ''){this.value ='Nhập vào Họ tên thân nhân/Họ tên cán bộ/Mã cán bộ'}",  :style => "color: gray; width: 300px; padding: 2px;"}%>
      <%= submit_tag "Tìm Kiếm",:disable_with => "Đang tìm kiếm...", :style => "padding: 3px 10px;" %>

  <% end %>
</div>

<h2>Thân nhân cán bộ</h2>

<%= render :partial => 'casein/than_nhans/table', :locals => { :than_nhans => @than_nhans } %>

<%= will_paginate @than_nhans %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "add"}Thêm mới thân nhân".html_safe, new_casein_than_nhan_path %></li>
    <li><%= link_to "#{casein_show_icon "add"}Thêm mới thân nhân từ file excel".html_safe, import_from_excel_casein_than_nhans_path %></li>
    <li><%= link_to "#{casein_show_icon('excel')}Xuất danh sách ra excel".html_safe, casein_than_nhans_path({:format => :xls, :keyword => params["keyword"], :commit => params["commit"], :utf8 => params["utf8"]}) %></li>
<% end %>