<!-- Scaffolding generated by Casein v.3.1.11 -->
<div class="blk-share">
<%= form_tag casein_bac_luongs_path, :method => "get" do %>
<h2>Danh sách ngạch - bậc lương
	
	<%= select_tag  "nid", 
                    options_from_collection_for_select( Ngach.all, 
                                                        "id", 
                                                        "ten_ngach", 
                                                        :selected => params["nid"] || @default_select_ngach
                                                      ),
                    {
                    	:prompt => "Tất cả",
                    	:onchange => "this.form.submit();",
                    	:class => "select_link_top",
                    	:title => "Xem theo từng ngạch"
                	}  %>
   
</h2>
<% end %>



<% bac_luongs = @bac_luongs %>
<% if !params[:view].blank? %>
	<% bac_luongs = @bac_luongs_group %>
<% end %>
<%= render :partial => 'casein/bac_luongs/table', :locals => { :bac_luongs => bac_luongs } %>

<% if params[:view].blank? %>
	<%= will_paginate @bac_luongs,:previous_label => '&#8592; Trước', :next_label => 'Sau &#8594;' %>
<% end %>

</div>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "add"}Thêm mới ngạch - bậc lương".html_safe, new_casein_bac_luong_path %></li>
    <li><%= link_to "#{casein_show_icon "import"}Thêm mới ngạch - bậc lương từ excel".html_safe, import_from_excel_casein_bac_luongs_path %></li>
    <li><%= link_to "#{casein_show_icon('excel')}Xuất danh sách ra excel".html_safe, casein_bac_luongs_path({:format => :xls, :keyword => params["keyword"], :commit => params["commit"], :utf8 => params["utf8"]}) %></li>
<% end %>