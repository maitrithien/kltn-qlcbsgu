<!-- Scaffolding generated by Casein v.3.1.11 -->
<% if @can_bo_cong_tac %>

	<script type="text/javascript">
	    <% if params[:ma_cb] %>
	    $(document).ready(function(){
	    	var obj = document.getElementById("ma_cb");
	    	obj.setAttribute("readonly");
	    });
	    <% end %>
	</script>

 

	<div class="blk-share">
	<h2>Thêm mới chi tiết công tác</h2>
	<%= form_for @can_bo_cong_tac, :url => casein_can_bo_cong_tacs_path do |f| %>
		
		<%= render :partial => 'casein/can_bo_cong_tacs/form', :locals => { :f => f } %>
		
		<p class="submits">
			<%= link_to 'Trở về', casein_can_bo_cong_tacs_path, :class => 'cancel' %> hoặc
			<%= f.submit "Thêm mới chi tiết công tác", :class => "update" %>
		</p>

	<% end %>

	</div>
<% end %>
<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "table"}Danh sách cán bộ".html_safe, casein_can_bo_cong_tacs_path %></li>
<% end %>